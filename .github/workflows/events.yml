name: events

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          pip install -r req/requirements.txt
          python -m pip install --upgrade pip
          pip install requests pycryptodome

      - name: Run
        run: |
          python src/roxie_scraper.py
          python scripts/generate-sport-m3u.py
          python src/Sportzx.py

      - name: Commit updated outputs
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add roxie_streams.json roxie_streams.m3u8
          git diff --cached --quiet || git commit -m "Update Roxie stream outputs"
          git add SPORTS.m3u
          git commit -m "Auto-update SPORTS M3U playlist [skip ci]" || echo "No changes to commit"
          git add Sportzx.m3u8
          git diff --cached --quiet || git commit -m "Aggiornato file M3U8 con nuovi flussi"
          git push
