name: Update Live TV & Events Playlist

on:
  workflow_dispatch: 
  schedule:
    - cron: '*/10 * * * *'    

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - run: echo "Cron job is running"

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: pip install -r req/requirements.txt

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests playwright
    - name: Install Playwright Browsers & OS Dependencies
      run: playwright install chromium --with-deps

    - name: Run Scraper Script
      run: python src/channels.py

    - name: Run Scraper Script 
      run: python src/liveevents.py

    - name: Run scraper
      run: python src/roxie_scraper.py

    - name: Commit and Push Playlist
      run: |
        git config --local user.name "github-actions[bot]"
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add live_tv_channels.m3u
        git add liveevents.m3u
        git add roxie_streams.json roxie_streams.m3u8
        
        # Check if there are actually changes to commit to avoid empty commit errors
        if git diff-index --quiet HEAD --; then
          echo "No changes to playlist, skipping commit."
        else
          git commit -m "Auto-update live_tv_channels.m3u"
        # The '|| true' prevents the workflow from failing if no changes were made
        git commit -m "Auto-update liveevents.m3u" || true
        git diff --cached --quiet || git commit -m "Update Roxie stream outputs"
        
        git push
          
          # Pull remote changes and rebase BEFORE pushing to prevent rejection errors
          git pull --rebase origin main
          git push origin main
        fi
